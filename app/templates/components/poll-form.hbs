{{yield}}
<form class="form">

{{input value=poll.topic placeholder="Umfrage Thema" action=""}}
<ul>

{{#each poll.options as |option|}}
<li>{{option.title}}</li>
{{/each}}

{{simple-form placeholder="Auswahlmöglichkeit" buttonText="Möglichkeit Hinzufügen" iconShow="plus" iconHide="minus" create="createOption"}}
<button type="submit" class={{if createable 'ok' 'disabled'}} {{action "savePoll"}}>Speichern</button>
</ul>
</form>
