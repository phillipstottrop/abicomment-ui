{{#if quote.text}}
<hr class="border">
<li>

  <h4 class="quoted">{{quote.quoted}}</h4>
  <p class="quote-text">
    {{quote.text}}
  </p>
  {{#if administratable }}
    <a {{action 'delete'}} class="action">
      <i class="delete quote icon"></i>
    </a>
   {{/if}}
</li>
{{/if}}
{{yield}}
