{{!--{{#if user.attendings }}--}}

  {{#unless currentUser}}
      {{simple-form create="create" placeholder="Kommentar" openText="kommentieren" }}
  {{/unless}}
    {{comment-listing comments=comments blur="blur"}}

{{!--{{/if}}--}}

{{yield}}
